import { AppRegistry } from 'react-native'

import App from './src/App'

// Must match MainActivity.getMainComponentName() on Android ("cloudipsp").
AppRegistry.registerComponent('cloudipsp', () => App)
